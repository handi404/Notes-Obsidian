// 内存 = RAM（街道）内的字节数组
// 内存块 = 内存（房子）内的单个单位（字节），用于保存某些值（人）
// 内存地址 = 内存块所在的地址（房屋地址）

---

```c
#include <stdio.h>
 
int main()
{
   char a;
   char b[1];

   printf("%d bytes\n", sizeof(a));
   printf("%d bytes\n", sizeof(b));

   printf("%p\n", &a);
   printf("%p\n", &b);

   return 0;
}
```