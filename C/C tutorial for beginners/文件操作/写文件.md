```c
#include <stdio.h>

int main()
{
   // 写入/追加文件 WRITE/APPEND A FILE
   FILE *pF = fopen("C:\\Users\\Cakow\\Desktop\\test.txt", "w");

   fprintf(pF, "Spongebob Squarepants");

   fclose(pF);
   
   // 删除文件 DELETE A FILE
   /*
   if(remove("test.txt") == 0)
   {
      printf("That file was deleted successfully!");
   }
   else
   {
      printf("That file was NOT deleted!");
   }
   */
   return 0;
}
```