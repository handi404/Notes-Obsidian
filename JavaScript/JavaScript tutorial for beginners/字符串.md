```js
const name = "dev";
const age = 24;

console.log("hello this's me " + name + " my age is " + age);
console.log(`hello this's me ${name} my age is ${age}`);

console.log("hello " + "world");

const combin = name + age;

console.log(combin);

console.log(typeof combin); //string + number = string
```